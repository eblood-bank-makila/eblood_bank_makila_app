import 'package:flutter/material.dart';

class ColorPages {
  static const COLOR_PRINCIPAL = Color(0XFFFF0000);
  static const COLOR_BLANCHE = Color(0XFFFFFFFF);
  static const COLOR_BLUE = Color(0XFF009DFF);
  static const COLOR_JAUNE = Color(0XFFEEC91C);
  static const COLOR_TRANSPARENT = Colors.transparent;
  static const COLOR_GRIS = Colors.grey;
  static const COLOR_NOIR = Colors.black;
  static const COLOR_CARD =Color(0xFFf1f2f6);
  static const COLOR_FOND=Color(0xFFE6E6E6);
}
